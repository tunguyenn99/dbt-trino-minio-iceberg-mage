dbt_trino_project:
  outputs:

    dev:
      type: trino
      method: none  # optional, one of {none | ldap | kerberos}
      user: tunguyenn99
      password: ''  # required if method is ldap or kerberos
      database: tpch
      host: localhost
      port: 8080
      schema: sf1000
      threads: 4

    prod:
      type: trino
      method: none  # optional, one of {none | ldap | kerberos}
      user: tunguyenn99
      password: ''  # required if method is ldap or kerberos
      database: tpch
      host: localhost
      port: 8080
      schema: sf1000
      threads: 4

  target: dev